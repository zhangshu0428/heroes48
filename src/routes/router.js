import VueRouter from 'vue-router';
import Vue from 'vue';

Vue.use(VueRouter);

//创建路由对象

var router = new VueRouter({
    routes: [

        // {name: 'heroes', path: '/heroes', component: }

    ]
})

//导出路由
export default router;
